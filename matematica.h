#ifndef _LIBMATEMATICA_H
#define _LIBMATEMATICA_H

#include <math.h>
#include <stdio.h>

/**
 * Funções criadas para calcular respectivamente
 * o seno, cosseno e tangente de um
 * @param angulo_radianos angulo em radianos
*/
double calcular_seno(double angulo_radianos);

double calcular_cosseno(double angulo_radianos);

double calcular_tangente(double angulo_radianos);

#endif